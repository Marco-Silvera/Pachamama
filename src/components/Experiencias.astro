---
import { FeaturedImageGallery } from "./FeaturedImageGallery.jsx";
import experienciasData from "../db/experiencias.json";

const { nombre } = Astro.props.circuito;
const experiencias = experienciasData.find((e) => e.nombre === nombre);
const images = experiencias ? experiencias.imagenes : [];
---

<div>
  <h2>Experiencias</h2>
  {
    images.length > 0 ? (
      <FeaturedImageGallery images={images} />
    ) : (
      <p>No hay imágenes disponibles para este circuito.</p>
    )
  }
</div>
