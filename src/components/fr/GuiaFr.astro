---
import guiasData from "../../db/fr/guias.json";
---

<section
  class="flex flex-col gap-5 md:gap-10 self-center max-w-[1000px] w-full py-20 px-5"
>
  <div class="flex flex-col gap-5 md:gap-10">
    <h2 class="text-3xl md:text-6xl font-bold uppercase text-center">
      Nos guides
    </h2>
    <p class="text-base font-normal text-center text-pretty">
      Ils sont en formation constante pour faire de votre voyage un des les meilleures expériences de leur vie. Nous vous présentons ci-dessous :
    </p>
  </div>
  <section class="flex flex-wrap justify-center gap-10">
    {
      guiasData.map((guia) => (
        <article class="w-full max-w-[300px] flex flex-col gap-5 items-center">
          <img
            class="aspect-[2/3] object-cover rounded-lg"
            src={guia.imagen}
            alt={`Guia ${guia.nombre}`}
          />
          <div class="flex flex-col gap-5">
            <h3 class="text-xl md:text-2xl font-bold uppercase">
              {guia.nombre}
            </h3>
            <p class="text-sm md:text-base font-medium text-[#7A7A7A] text-pretty">
              {guia.descripcion}
            </p>
          </div>
        </article>
      ))
    }
  </section>
</section>
